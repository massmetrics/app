<%= "Categories for: #{@product.title}" %>

<%= form_tag(admin_product_path(@product), method: :patch) do %>
  <%= select_tag(:category, options_from_collection_for_select(Category.all, 'id', 'name', @selection), prompt: "Select something") %>
  <%= submit_tag 'Update' %>
<% end -%>