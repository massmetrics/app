<% @submissions.each do |submission| %>
  <h3>Submission</h3>
  SKU: <%= submission.sku %>
  Category: <%= submission.category %>
  <%= link_to 'Add', edit_admin_submission_path(submission)  %>
<% end %>