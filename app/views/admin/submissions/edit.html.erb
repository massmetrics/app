<%= form_for([:admin, @submission]) do |f| %>
  <%= f.text_field :sku %>
  <%= f.text_field :category %>
  <%= f.submit 'Add' %>

<% end %>

<%= link_to 'Amazon detail page', @amazon_detail_page, id: 'amazon-link' %>